import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
    titleText: {
        fontSize: 25,
    },
    subtitleText: {
        fontSize: 16,
    },
    smallText: {
        fontSize: 12,
    },
});

export default commonStyles;
