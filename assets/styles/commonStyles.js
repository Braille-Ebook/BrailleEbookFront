import { StyleSheet } from 'react-native';

const commonStyles = StyleSheet.create({
    titleText: {
        fontSize: 25,
    },
    subtitleText: {
        fontSize: 16,
    },
});

export default commonStyles;
